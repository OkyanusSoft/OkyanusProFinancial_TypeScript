# ═══ إعدادات الخادم المركزي — النظام المالي المتكامل ═══
PORT=4000
WS_PATH=/ws

# ── الاتصال المركزي MySQL ──
DB_HOST=localhost
DB_PORT=3306
DB_USER=erp_admin
DB_PASS=ChangeMe!2026
DB_NAME=okyanus_ifs
# حجم حزمة الاتصالات — يكفي لأكثر من 100 مستخدم متزامن
DB_POOL_LIMIT=40
DB_QUEUE_LIMIT=200

# ── الأمان ──
JWT_SECRET=ضع-سراً-طويلاً-عشوائياً-هنا
JWT_EXPIRES=8h
MASTER_PIN_HASH=$2a$10$مثال-تجزئة-الرقم-السري-للمالك
RATE_LIMIT_PER_MIN=240

# ── المزامنة ──
# الفترة (مللي ثانية) التي يعتبر بعدها الجهاز خاملاً
DEVICE_STALE_MS=15000
